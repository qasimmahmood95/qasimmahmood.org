# TODO — needs Q's input

Everything below needs real details from you; nothing has been invented.
Each item also has a matching `<!-- TODO -->` comment in `index.html` where relevant.

## Facts to fill in

- [ ] **LinkedIn URL** — I've guessed `linkedin.com/in/qasimmahmood95` in the hero and contact sections. Confirm or correct (appears twice in `index.html`).
- [ ] **About — degree**: confirm MEng subject and graduation year (currently just "MEng from Imperial College London").
- [ ] **Experience — current role**: start year, and how you want the employer named (currently the generic "Digital asset custody firm").
- [ ] **Experience — current role bullets**: add concrete achievements/metrics (release cadence, coverage, incidents caught, scale). The three existing bullets only restate facts from the brief.
- [ ] **Experience — earlier roles**: the brief only covers your current role. Fill in previous employers, titles, dates, and bullets — the second timeline entry is a placeholder.

## To review

- [ ] **All copy** — hero line, about narrative, contact blurb are first drafts for you to edit.
- [ ] **Certification names** — I've used the official long names (e.g. "AWS Certified Solutions Architect — Associate", "ISTQB Foundation Level — Agile Tester"). Check they match your actual certs, especially the ISTQB Agile one and "AUSCIF Islamic Finance".
- [ ] **Email** — `qasimm999@gmail.com` is assembled at runtime in `js/main.js`. Confirm this is the address you want public.
- [ ] **Favicon** — currently a minimal teal "Q" monogram (inline SVG). Replace if you want something else.

## Before/at launch

- [ ] Create the GitHub repo under `qasimmahmood95` and push (`git remote add origin … && git push -u origin main`).
- [ ] Set up Cloudflare Pages + custom domain (steps in README).
- [ ] Run Lighthouse against the deployed site (local static-server numbers aren't representative).
- [ ] `BRIEF.md` is gitignored as a working note — delete or un-ignore as you prefer.
