# TODO: needs Q's input

Everything below needs real details; nothing has been invented.
Each item also has a matching `<!-- TODO -->` comment in `index.html` where relevant.

## Facts to fill in

- [ ] **LinkedIn URL**: `linkedin.com/in/qasimmahmood95` is a guess, used in the hero and contact sections. Confirm or correct (appears twice in `index.html`).
- [ ] **About / degree**: confirm MEng subject and graduation year (currently just "MEng from Imperial College London").
- [ ] **Experience / current role**: start year, and how to name the employer (currently the generic "Digital asset custody firm").
- [ ] **Experience / current role bullets**: add concrete achievements and metrics (release cadence, coverage, incidents caught, scale). The three existing bullets only restate facts from the brief.
- [ ] **Experience / earlier roles**: the brief only covers the current role. Fill in previous employers, titles, dates and bullets; the second timeline entry is a placeholder.

## To review

- [ ] **All copy**: hero line, about narrative and contact blurb are first drafts to edit.
- [ ] **Certification names**: check they match the actual certs, especially the ISTQB Agile one and "AUSCIF Islamic Finance".
- [ ] **Email**: `qasimm999@gmail.com` is assembled at runtime in `js/main.js`. Confirm this is the address to make public.
- [ ] **Favicon**: currently a minimal teal "Q" monogram (inline SVG). Replace if wanted.

## Before/at launch

- [ ] Create the GitHub repo under `qasimmahmood95` and push (`git remote add origin ... && git push -u origin main`).
- [ ] Set up Cloudflare Pages and the custom domain (steps in README).
- [ ] Run Lighthouse against the deployed site (local static-server numbers aren't representative).
- [ ] `BRIEF.md` is gitignored as a working note. Delete or un-ignore as preferred.
